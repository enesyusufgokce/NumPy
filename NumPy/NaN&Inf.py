import numpy as np

print(np.nan)      # not a number
# in a huge datasets, if some values or some column are missing, we fill it up with nan, or drop the nan
print(np.inf)      # infinity
# if we get results like division by zero. exception fırlatmak yerine infinity value döndürebiliriz

print(np.sqrt(-1))
print(np.array([10]) / 0)
